# Python dependencies of code/grace_pipeline/ and code/estimator_global.py.
# Direct imports only, pinned to the versions that produced the shipped
# outputs (Python 3.12.4). gravity-toolkit pulls its own geodesy stack.
cdsapi==0.7.7
earthaccess==0.17.0
geopandas==1.1.3
gravity-toolkit==1.2.5
netCDF4==1.7.4
numpy==2.4.4
pandas==2.3.3
rasterio==1.5.0
rasterstats==0.21.0
requests==2.33.1
scipy==1.17.1
shapely==2.1.2
xarray==2026.4.0
# rasterstats (climate_zones.py only) was absent from the environment that
# generated the other shipped outputs and was installed afterwards.
